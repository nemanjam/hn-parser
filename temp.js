function replaceUrlAndFollowing(str) {
    // Regular expression to match URLs
    const urlRegex = /((?:https?|ftp):\/\/[\n\S]+)/gi;

    // Replace URL and following whitespace
    return str.replace(urlRegex, '').trim();
}.

let threads = document.querySelectorAll('tr.athing span.titleline a')

const monthRegex = /\((\w+)/;
const match = text.match(regex);
const month = match[1];


